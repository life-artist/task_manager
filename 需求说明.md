# 任务管理应用 — 需求说明

本文档把当前约定的功能和选型固定下来，后续开发和验收都以这里为准。

---

## 1. 产品目标

做一个运行在浏览器里的**个人任务看板**：能增删改查任务，用三列看板看状态，支持拖拽改状态，能切换深色模式，刷新页面后数据还在。

本阶段**不做**账号登录、多人协作、后端接口。所有数据只存在当前浏览器里。

---

## 2. 功能需求

### 2.1 任务增删改查

每条任务包含：

| 字段 | 是否必填 | 说明 |
|------|----------|------|
| 标题 | 必填 | 不能为空，也不能只输入空格 |
| 描述 | 选填 | 可为空 |
| 状态 | 系统维护 | 见 2.2 |
| 优先级 | 必选 | 见 2.3 |
| id | 系统生成 | 用于区分每条任务，用户看不到 |

操作：

- **增**：通过表单新建。标题为空时不能保存，并给出提示。
- **查**：在看板上按状态分列展示卡片。
- **改**：可修改标题、描述、优先级；状态可通过拖拽（或后续补充的移动操作）修改。
- **删**：可删除任务，删除前需要确认，避免误点。

### 2.2 三种状态

| 状态值 | 中文名 | 看板列 |
|--------|--------|--------|
| `todo` | 待办 | 第一列 |
| `doing` | 进行中 | 第二列 |
| `done` | 完成 | 第三列 |

新建任务默认状态为 **待办**。

### 2.3 优先级三档

| 优先级 | 中文名 | 颜色 |
|--------|--------|------|
| `high` | 高 | 红 |
| `medium` | 中 | 黄 |
| `low` | 低 | 绿 |

卡片上要用颜色明确标出优先级（色条、色点或标签均可，但三种必须一眼能分清）。

### 2.4 看板视图

- 页面主体是三列卡片：待办 / 进行中 / 完成。
- 每列展示该状态下的任务卡片。
- **拖拽卡片到另一列，状态立刻变成该列对应状态。**
- 拖拽完成后，列表和数据都要更新，不能只是视觉上挪了位置。

### 2.5 深色模式

- 提供一键切换（亮色 / 深色）。
- 把用户的选择记住（同样用浏览器本地存储）。
- 刷新页面后，主题仍是上次选的，而不是回到默认亮色。

### 2.6 数据持久化

- 任务列表存在浏览器 `localStorage`。
- 刷新、关闭标签页再打开，任务都还在。
- 换浏览器或清站点数据后会丢失（这是 localStorage 的正常行为，不是 bug）。

---

## 3. 技术选型（必须遵守）

| 用途 | 技术 |
|------|------|
| 前端框架 | Vue 3 |
| 构建工具 | Vite |
| 样式 | Tailwind CSS |
| 数据存储 | localStorage |

补充约定（方便学习、也方便以后对照代码）：

- 使用 Vue 3 的 **Composition API**（`<script setup>`），这是现在 Vue 3 的主流写法。
- 单页应用即可，不需要 Vue Router（目前只有一个看板页）。
- 拖拽优先用轻量方案实现（HTML5 拖拽，或一个专门的拖拽库），不要上大型看板套件。
- 不引入后端、数据库、登录、TypeScript（保持入门友好；如后续你要求再加）。

---

## 4. 界面范围（本阶段）

需要有：

- 顶栏：应用名称 + 深色模式开关 + 新建任务入口
- 三列看板
- 任务卡片（标题、描述、优先级标识）
- 新建 / 编辑任务的表单（弹窗或侧栏均可）
- 空列提示（某一列没有任务时，不要一片空白让人以为坏了）

本阶段不做：

- 用户系统、云同步
- 截止日期、标签、评论、附件
- 搜索、筛选、排序的复杂面板（若步骤有余力可加最简单的按优先级排序，非必须）
- 移动端原生 App

---

## 5. 完成标准（全部做完时）

打开应用后，应能一次性做到：

1. 新建一条只有标题的任务，出现在「待办」。
2. 新建时不填标题，无法保存。
3. 编辑描述和优先级，卡片立刻更新。
4. 把卡片从「待办」拖到「进行中」，再拖到「完成」，列和状态都对。
5. 删除一条任务，确认后卡片消失。
6. 刷新页面，上述任务还在。
7. 切换深色模式，刷新后主题保持。
8. 高 / 中 / 低优先级在卡片上分别是红 / 黄 / 绿。

逐步怎么验收，见 `分步实施计划.md`。
