# 任务管理应用 — 分步实施计划

这份计划把开发拆成 **你可以亲眼看见变化** 的小步骤。  
每一步都有：**这一步在学什么、做完会长什么样、你怎么验收**。

约定：

- 每一步做完先验收，通过了再进入下一步。不要一次堆完所有功能。
- 每一步结束后，用浏览器看实际页面，而不是只看代码有没有报错。
- 开发命令统一在项目根目录 `C:\Users\boyata\Desktop\task_manager` 执行。
- 本地预览地址一般是：`http://localhost:5173`（Vite 默认端口）。

配套需求见 `需求说明.md`。

---

## 你需要先具备的环境

电脑上要有 **Node.js**（建议 18 或以上）。

在终端执行：

```powershell
node -v
npm -v
```

能显示版本号即可。没有的话先安装 Node.js，再开始步骤 1。

---

## 步骤 1：用 Vite 搭出 Vue 3 空项目

**目标：** 浏览器里能打开一个 Vue 应用，证明框架跑通了。

**这一步你会接触：**

- **Vite**：开发时的启动器。改代码后页面几乎立刻刷新。
- **Vue 3**：页面由「组件」拼起来。现在先用官方脚手架生成的默认页。

**建议命令（你自己敲）：**

因为当前目录里已经有 Git 和 README，不要用会覆盖整个目录的方式乱生成。优先在当前目录初始化：

```powershell
cd C:\Users\boyata\Desktop\task_manager
npm create vite@latest . -- --template vue
npm install
npm run dev
```

如果脚手架问是否覆盖已有文件，只覆盖它需要的项目文件，**不要删掉** `需求说明.md`、`分步实施计划.md` 和 `.git`。

**做完后你应看到：**

- 终端提示 Local 地址（通常是 `http://localhost:5173`）
- 浏览器打开后是 Vite + Vue 的默认欢迎页（计数按钮、Logo 等）

**你怎么验收：**

1. 浏览器能打开页面，不是空白或报错。
2. 点默认页上的按钮，数字会增加（说明 Vue 的响应式已经在工作）。
3. 项目里出现 `package.json`、`vite.config.js`、`src/main.js`、`src/App.vue`、`index.html`。

**本步结束时还没有：** Tailwind、看板、任务数据。这些都正常。

---

## 步骤 2：接入 Tailwind CSS

**目标：** 之后用 class 名字就能改样式，不再手写大段 CSS。

**这一步你会接触：**

- **Tailwind CSS**：在 HTML/Vue 标签上写 `bg-slate-900`、`text-white` 这类工具类来排版和上色。

**做完后你应看到：**

把欢迎页或 `App.vue` 里随便一块改成明显的 Tailwind 样式，例如深色背景、大标题。保存后页面颜色/布局立刻变。

**你怎么验收：**

1. `npm run dev` 仍然正常。
2. 你写的 Tailwind class **真的生效**（例如背景变成深灰、文字变大）。
3. 如果 class 写了但页面完全没变，说明 Tailwind 没接到 Vue 文件，停在这一步修，不要往下做。

**本步结束时还没有：** 看板界面。可以先把默认 Vite Logo 页换成一个很简单的「任务管理」标题，方便后面搭布局。

---

## 步骤 3：画出应用骨架（顶栏 + 三列空看板）

**目标：** 先有「房子的墙」，再往里面放家具。

**界面应包含：**

- 顶部：应用名「任务管理」
- 主体：三列，标题分别是 **待办 / 进行中 / 完成**
- 每列是一个空容器（可以写「暂无任务」）

**这一步你会接触：**

- Vue 单文件组件：一个 `.vue` 文件里同时写结构、脚本、样式。
- 用 Tailwind 做三列布局（桌面三列并排；窄屏幕可以先允许纵向堆叠）。

**你怎么验收：**

1. 打开页面，一眼能看出三列看板，而不是默认 Vite 欢迎页。
2. 三列标题正确：待办、进行中、完成。
3. 暂时没有卡片也没关系，空列要看得到，不能像页面坏了一样全白。

**本步结束时还没有：** 真实任务数据、按钮功能。顶栏可以先放两个不能点的按钮占位：「新建任务」「深色模式」。

---

## 步骤 4：用假数据渲染任务卡片

**目标：** 先让卡片「长对」，再接增删改和存储。

**任务在代码里的形状（后续步骤都沿用）：**

```js
{
  id: "1",
  title: "示例：写需求文档",
  description: "描述可以没有",
  status: "todo",      // todo | doing | done
  priority: "high"     // high | medium | low
}
```

**卡片上必须能看出：**

- 标题
- 描述（没有描述时可以不显示描述区域）
- 优先级颜色：高=红、中=黄、低=绿

每列只显示对应 `status` 的卡片。

**你怎么验收：**

1. 在代码里写死 3～5 条示例任务，分别落在三列里。
2. 刷新后这些示例卡片还在（这一步它们写在代码里，刷新当然在；下一步才是用户添加的数据）。
3. 红色/黄色/绿色能对上高/中/低，不要靠猜。
4. 「待办」列里不应出现 `status: "done"` 的卡片。

**本步结束时还没有：** 新建表单、拖拽、localStorage。卡片还是写死的。

---

## 步骤 5：新建任务（标题必填、描述选填）

**目标：** 你能从页面上真正「增加」一条任务。

**需要有：**

- 「新建任务」打开表单（弹窗即可）
- 标题输入框（必填）
- 描述输入框（选填）
- 优先级选择：高 / 中 / 低
- 保存 / 取消

规则：

- 标题为空或只有空格 → 不能保存，并提示「请填写标题」
- 保存成功 → 表单关闭，新卡片出现在 **待办**
- 默认优先级可以是「中」

**这一步你会接触：**

- Vue 的数据绑定：输入框里敲的字，会同步到变量里。
- 点击事件：点按钮后执行函数，往任务列表里追加一条。

**你怎么验收：**

1. 填标题「买菜」，保存，「待办」里出现这张卡片。
2. 标题留空点保存，没有新卡片，有错误提示。
3. 只填标题、不填描述，也能保存。
4. 分别选高/中/低，卡片颜色对。
5. **先不要刷新。** 这一步刷新后新任务消失是正常的（还没做 localStorage）。

---

## 步骤 6：编辑和删除

**目标：** 改得了、删得掉。

**编辑：**

- 卡片上能进入编辑（点卡片或「编辑」按钮）
- 可改标题、描述、优先级
- 标题仍必填
- 保存后同一张卡片更新，不要变成一条新任务（`id` 不变）

**删除：**

- 有删除入口
- 删除前确认（浏览器 `confirm` 或自定义确认框都可以）
- 确认后卡片从看板消失

**你怎么验收：**

1. 把「买菜」改成「买菜和牛奶」，卡片标题马上变。
2. 改优先级后颜色马上变。
3. 取消编辑，内容不应被改掉。
4. 点删除再取消，卡片还在；确认删除后卡片消失。
5. 刷新后这些改动仍可能丢失（下一步才持久化）。

---

## 步骤 7：先用按钮改状态（为拖拽打底）

**目标：** 先保证「改状态」这条数据链路是通的。拖拽只是换一种操作方式。

每张卡片可提供简单操作，例如：

- 待办 → 「开始」→ 进行中
- 进行中 → 「完成」→ 完成（也可以「退回待办」）
- 完成 → 「重开」→ 待办

**你怎么验收：**

1. 点「开始」，卡片从待办列消失，出现在进行中列。
2. 再点「完成」，出现在完成列。
3. 三列数量加起来始终等于任务总数，没有卡片复制或失踪。

**为什么单独做这一步：**  
拖拽容易把「移动动画」和「改数据」搅在一起。数据先对了，再加拖拽，出问题更好查。

---

## 步骤 8：拖拽卡片改状态

**目标：** 拖到哪一列，状态就变成哪一列。

**行为：**

- 按住卡片，拖到另一列松开
- `status` 更新为该列状态
- 卡片渲染到新列
- 同一列内拖动（如果实现了排序）不要改状态；没做列内排序也可以，只要跨列改状态正确即可

**你怎么验收：**

1. 待办拖到进行中：卡片换列，且刷新前再拖回来也能回去。
2. 拖到完成列，只出现在完成，不会在两列各有一张。
3. 拖拽过程中页面不要整页崩溃、卡片不要「粘」在鼠标上放不下来。
4. 步骤 7 的按钮如果还留着，结果应和拖拽一致（都是改同一个 `status` 字段）。

---

## 步骤 9：用 localStorage 记住任务

**目标：** 刷新不丢失任务。

**这一步你会接触：**

- **localStorage**：浏览器给每个网站留的一小块本地记事本。关标签页还在，清站点数据会没。
- 任务列表一变化就写入；页面一打开就读出。

建议键名：`task-manager.tasks`（避免和别的练习项目撞名）。

**你怎么验收（这是最关键的一次）：**

1. 新建 2 条任务，改 1 条，删 1 条，再拖 1 条到「完成」。
2. 按浏览器刷新（F5）。
3. 刚才的结果全部还在，状态和优先级都对。
4. 打开开发者工具 → Application（应用）→ Local Storage，能看到对应键和 JSON 数据。
5. 代码里的示例假数据不应每次刷新都把你的真实任务覆盖掉。

**对比：** 主题切换还没做的话，刷新后主题可以回到亮色，这不算本步失败。

---

## 步骤 10：深色模式，并记住选择

**目标：** 一键切换，刷新后还是你选的主题。

**需要有：**

- 顶栏开关（按钮或切换器）
- 亮色、深色两套配色，看板和卡片都要跟着变，不要只有背景黑了、字还是黑的（看不清）
- 选择写入 localStorage（建议键名：`task-manager.theme`）
- 页面加载时先读主题，避免先闪一下亮色再变深色（能做到最好，做不到可放最后打磨）

**你怎么验收：**

1. 点开关，整页立刻在亮/深之间切换。
2. 深色下标题、卡片、输入框文字都清晰可读。
3. 切到深色 → 刷新 → 仍是深色。
4. 再切回亮色 → 刷新 → 仍是亮色。
5. 改主题不应把任务数据清掉。

---

## 步骤 11：收尾打磨（功能齐全后再做）

**目标：** 用起来像一个完整小应用，而不是半成品。

建议打磨项（都可以在页面上直接看出来）：

- 空列文案：「这一列还没有任务」
- 表单取消、点遮罩关闭弹窗
- 卡片描述过长时截断，编辑时仍能看到全文
- 顶栏在深色/亮色下都好看
- 窗口变窄时三列不要挤成无法使用（至少能纵向滚动看完三列）

**最终总验收（对照 `需求说明.md` 第 5 节）：**

1. 标题必填、描述选填
2. 三种状态、三列看板
3. 优先级红/黄/绿
4. 拖拽改状态
5. 增删改查都可用
6. 深色模式可切换且刷新保持
7. 任务刷新不丢失

全部通过，这一版应用就算完成。

---

## 建议的文件结构（做完后大致如此）

实际文件名可以微调，但职责尽量分开，方便你对照每一步：

```text
task_manager/
  index.html
  package.json
  vite.config.js
  src/
    main.js                 # 应用入口
    App.vue                 # 总布局：顶栏 + 看板
    style.css               # Tailwind 入口
    components/
      BoardColumn.vue       # 一列看板
      TaskCard.vue          # 一张卡片
      TaskForm.vue          # 新建/编辑表单
      ThemeToggle.vue       # 深色模式开关
    composables/
      useTasks.js           # 任务增删改查 + localStorage
      useTheme.js           # 主题切换 + localStorage
  需求说明.md
  分步实施计划.md
```

你还不熟这些概念也没关系：步骤 1～4 会先见到 `App.vue`，后面再按需要拆组件。

---

## 我们怎么配合

你说从哪一步开始，我就只做那一步，做完告诉你打开哪里看、点什么、应该出现什么。  
你验收通过后，再说「做下一步」。

不建议一次做完全部步骤——那样你很难把「Vite / Vue / Tailwind / localStorage」各自的效果分开看清楚。
